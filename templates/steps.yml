# templates/steps.yml

steps:
- powershell: |
    $projectName = 'Get-SMARTReport'
    git clone "https://github.com/joeltimothyoh/$projectName"
    Push-Location $projectName
    Get-Location
    Import-Module "Modules/$projectName/Get-SMARTReport.psm1"
    Get-SMARTReport
